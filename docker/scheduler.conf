# Bind address. Not needed to be public
public_addr = "0.0.0.0:7634"

[client_auth]
type = "token"
# Your PC<->Scheduler token
token = "token1"

[server_auth]
type = "token"
# Builder<->Scheduler token
token = "token2"
